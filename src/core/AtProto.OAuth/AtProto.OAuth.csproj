<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>net8.0;net10.0</TargetFrameworks>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>true</IsPackable>
    <Description>atproto OAuth profile primitives for .NET: DPoP proof validation and rolling nonces, PKCE, EC P-256 JWK and RFC 7638 thumbprints, URL client_id metadata documents with SSRF-hardened fetching, server metadata, and DPoP-bound access tokens. Build a conformant atproto authorization server or client. Part of the atproto-dotnet package set.</Description>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="AtProto.OAuth.Tests" />
  </ItemGroup>

</Project>
